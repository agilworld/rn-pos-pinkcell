import React from "react"
import { 
    View, 
    Text, 
    SafeAreaView 
} from "react-native"

export default function Home(props:any): JSX.Element {
    return (<SafeAreaView>
        <View>
            <Text>Home screen</Text>
        </View>
    </SafeAreaView>)
}